//! Windows input acquisition: the Raw Input capture thread, device identity,
//! and the pointer-ballistics configuration needed to interpret what it
//! records.

pub mod ballistics;
pub mod devices;
pub mod raw_input;
